# kakeibo-app プロジェクト

## プロジェクト概要

レシート画像を Claude API で読み取り、家計簿に自動入力する Web アプリ。

## 技術スタック

- フロントエンド:React + Vite
- バックエンド:Node.js + Express
- グラフ表示:Chart.js
- データ保存:ブラウザの localStorage
- AI 連携:Claude API(モデル:claude-haiku-4-5)
- 環境変数管理:.env(.gitignore に追加)

## 機能要件

1. レシート画像をアップロードすると Claude API で内容を自動読み取り
2. 商品名・金額・日付を抽出して一覧表示
3. カテゴリ別(食費・日用品・外食など)に自動分類して集計
4. Chart.js でカテゴリ別の円グラフ・月別の棒グラフを表示
5. データはローカルストレージに保存(リロードしても消えない)

## セキュリティ要件

- Claude API のキーは .env で管理する
- .env は必ず .gitignore に追加してコミットしない
- API キーはブラウザから直接使わず、バックエンド経由で呼ぶ

## Git 運用ルール

- ファイルを変更したら、必ずすぐに git add → commit → push する
- コミットメッセージは日本語で、何を変更したか分かるように書く
- main ブランチに直接コミットしてOK(個人開発のため)

## デプロイ先

- GitHub にリポジトリを作成
- アカウント名:innova820712-gif

## 命名規約

- ファイル名・フォルダ名:小文字 + ハイフン区切り
  (例:upload-form.jsx, receipt-list.jsx)
- 変数名・関数名:キャメルケース
  (例:uploadImage, parseReceipt)
- コメントは日本語で記載する

## 開発時の注意

- コードを書く前に、まず構造の方針を説明してから書き始める
- ユーザー(財部一志)はパソコン初心者なので、難しい用語は避ける
- エラーが出たら、原因を分かりやすく説明する